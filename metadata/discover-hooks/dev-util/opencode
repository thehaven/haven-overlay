_schema = 1
# Upstream publishes the v1.x line as full GitHub releases; v2.x exists
# as tags only (beta line, packaged separately as dev-util/opencode2).
# use_latest_release keeps this package on the stable v1 releases.
source = "github"
github = "anomalyco/opencode"
use_latest_release = true
prefix = "v"