# Tasks: review-mcp-server-memory-vs-mem0

## 1. Drop the ebuild

- [ ] 1.1 `git rm -r dev-util/mcp-server-memory/` in haven-overlay.
- [ ] 1.2 `sudo -n ebuild dev-util/mcp-server-memory/* manifest` not applicable (deleting); verify
  `metadata/md5-cache/dev-util/mcp-server-memory-*` is allowed to lag (regenerated
  by `emaint sync -r haven-overlay`).
- [ ] 1.3 Drop the ebuild from `profiles/categories` if listed (verify
  `dev-util` listing is otherwise consistent with `gentoo-factory`).

## 2. Unmerge the installed package

- [ ] 2.1 `sudo -n emerge --unmerge dev-util/mcp-server-memory` to free the
  on-disk 16 MiB.
- [ ] 2.2 Optional cleanup: `sudo -n rm -f
  /usr/portage-distfiles/mcp-server-memory-*.gpkg.tar` to free another ~14 MiB
  in portage-distfiles.

## 3. Verify mesh and CLI clean

- [ ] 3.1 `mcp-mesh doctor` no longer flags `memory` mount failures.
- [ ] 3.2 `cat ~/.config/mcp-forge/registry.yaml | grep -A1 'name: memory'`
  shows nothing (entry removed). The 2026-09-04 state has it with
  `enabled: false`; the physical delete happens as part of this change.
- [ ] 3.3 `mcp-mesh mounts` no longer tries to spawn anything.

## 4. Documentation

- [ ] 4.1 Update `AGENTS.md` Verified gotchas section to remove the
  `memory` plugin reference (if any).
- [ ] 4.2 Update `metadata/use.local.desc` for `dev-util/mcp-meta` (which
  advertises other MCPs) to drop any reference to the dropped package.

## 5. Commit

- [ ] 5.1 Single conventional-commit: `dev-util/mcp-server-memory: drop
  unused npm package superseded by mem0-mcp`.
- [ ] 5.2 Do NOT commit md5-cache changes (regenerated by webhost).
